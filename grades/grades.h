#ifndef GRADES_H
#define GRADES_H

// Function to add a grade
int add_grade(int student_id, const char *subject, const char *grade);

// Function to list grades for a specific student
int list_grades(int student_id);

#endif